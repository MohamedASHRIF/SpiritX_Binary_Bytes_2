// utils/calculateStats.js

// utils/calculateStats.js
function calculateValue(runs, wickets) {
  return runs * 0.5 + wickets * 1.5; // Example logic
}

function calculatePoints(runs, wickets) {
  return runs * 10 + wickets * 15; // Example logic
}

module.exports = { calculateValue, calculatePoints };

  